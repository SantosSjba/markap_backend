# Environment
NODE_ENV=development
PORT=3000

# Database - MySQL (cPanel / hosting)
# Codifica caracteres especiales en la contraseña: % → %25, @ → %40, + → %2B, ^ → %5E
DATABASE_URL="mysql://markapho_admin:TU_PASSWORD_ENCODED@198.58.106.39:3306/markapho_bd_sistema"

# JWT Configuration
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=3600

# Frontend URL (for CORS and password reset links)
FRONTEND_URL=http://localhost:5173

# Recuperación de contraseña (correo de gerencia)
PASSWORD_RESET_GERENCIA_EMAIL=gerencia.markap@gmail.com

# Mail (SMTP) - Para recuperación de contraseña
# Si no se configuran, los correos se imprimen en consola (desarrollo)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=
MAIL_PASSWORD=
MAIL_FROM="MARKAP <noreply@markap.com>"

# Recuperación de contraseña
PASSWORD_RESET_EXPIRES_MINUTES=15

# Default Admin User (for seed)
ADMIN_EMAIL=admin@markap.com
ADMIN_PASSWORD=Admin123
ADMIN_FIRST_NAME=Admin
ADMIN_LAST_NAME=Sistema
