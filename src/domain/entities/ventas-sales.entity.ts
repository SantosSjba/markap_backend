/**
 * Referencia mínima al agregado de proceso de venta.
 * El repositorio aún expone filas enriquecidas como unknown[] hasta modelar DTOs de lectura.
 */
export class VentasSaleProcess {
  constructor(public readonly id: string) {}
}
